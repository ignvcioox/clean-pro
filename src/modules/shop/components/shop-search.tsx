import { Input } from '@/modules/shared/components/ui';

export const ShopSearch = () => {
   return (
      <Input
         type="search"
         placeholder="Buscar productos..."
         className="w-64"
      />
   );
};